<Window x:Class="AwesomeTodo.MainModule.Views.ShellWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        AllowsTransparency="False"
        TextOptions.TextFormattingMode="Display"
        RenderOptions.ClearTypeHint="Enabled"
        RenderOptions.BitmapScalingMode="NearestNeighbor"
        ResizeMode="CanResize"
        MinHeight="300"
        MinWidth="320"
        MaxHeight="900"
        MaxWidth="700"
        Height="520"
        Width="340"
        Title="ShellWindow">
    <Window.Effect>
        <DropShadowEffect Color="Gray" BlurRadius="20" Direction="-90" RenderingBias="Quality" ShadowDepth="4" />
    </Window.Effect>
    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="4" CaptionHeight="0" />
    </WindowChrome.WindowChrome>
    <Window.Resources>

        <Style TargetType="{ x:Type Button }">
            <Setter Property="Margin" Value="0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Height" Value="38" />
            <Setter Property="BorderThickness" Value="2" />
            <Setter Property="Foreground" Value="#e5e5e5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Tag" Value="{ x:Null }" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{ x:Type Button }">
                        <Border x:Name="ButtonBorder" Margin="{ TemplateBinding Margin }" Padding="{ TemplateBinding Padding }" BorderThickness="{ TemplateBinding BorderThickness }" SnapsToDevicePixels="True" CornerRadius="6">
                            <Border.Effect>
                                <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="4" Direction="270" Opacity="0.7" />
                            </Border.Effect>
                            <Border.Background>
                                <LinearGradientBrush StartPoint="0, 0" EndPoint="0, 1">
                                    <GradientStop Color="#313131" Offset="0" />
                                    <GradientStop Color="#232323" Offset="1" />
                                </LinearGradientBrush>
                            </Border.Background>
                            <Border.BorderBrush>
                                <LinearGradientBrush StartPoint="1, 0" EndPoint="1, 1">
                                    <GradientStop Color="#5f5f5f" Offset="0" />
                                    <GradientStop Color="#232323" Offset="0.8" />
                                </LinearGradientBrush>
                            </Border.BorderBrush>
                            <Grid Background="Transparent">
                                <fa:IconImage Icon="{ Binding Path=Tag, RelativeSource={ RelativeSource TemplatedParent }}" Foreground="{ TemplateBinding Foreground }" Width="20" MaxHeight="20" />
                                <ContentPresenter Visibility="Collapsed" />
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        
    </Window.Resources>
    <DockPanel MouseDown="DockPanel_MouseDown">
        <DockPanel.Background>
            <LinearGradientBrush StartPoint="1, 1" EndPoint="0, 0">
                <GradientStop Color="#5f5f5f" Offset="1" />
                <GradientStop Color="#171717" Offset="0" />
            </LinearGradientBrush>
        </DockPanel.Background>
        <Border DockPanel.Dock="Top" Padding="10" Height="60">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*" MinWidth="70" />
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="1*" MinWidth="70" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                    <Button Tag="AngleLeft"  Width="24" />
                    <Button Tag="AngleRight" Width="24" />
                </StackPanel>

                <Label Grid.Column="1" Content="{ Binding Path=Title, FallbackValue='AppName' }" FontSize="20" FontWeight="Bold" Foreground="#e5e5e5" VerticalAlignment="Center" HorizontalAlignment="Center" />

                <Button Grid.Column="2" Tag="PowerOff" Width="38" HorizontalAlignment="Right" />
            </Grid>
        </Border>

        <DockPanel DockPanel.Dock="Top" Margin="10">
            <TextBox DockPanel.Dock="Top" Height="38" Margin="0, 0, 0, 8" />
            <Grid DockPanel.Dock="Bottom">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Button Grid.Column="0" Tag="Add" />
                <Button Grid.Column="1" Tag="Remove" />
                <Button Grid.Column="2" Tag="Undo" />
            </Grid>
            <ListView DockPanel.Dock="Top" Margin="0, 0, 0, 8">
                <ListViewItem>Item #1</ListViewItem>
                <ListViewItem>Item #2</ListViewItem>
                <ListViewItem>Item #3</ListViewItem>
            </ListView>
        </DockPanel>
    </DockPanel>
</Window>
